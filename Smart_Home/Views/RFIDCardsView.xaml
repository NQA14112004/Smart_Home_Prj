<UserControl x:Class="Smart_Home.View.RFIDCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource BgMedium}" Margin="10" CornerRadius="8">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Danh sách Thẻ RFID" Foreground="White" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding CardsList}" SelectedItem="{Binding SelectedCard, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                        <DataGridTextColumn Header="Mã UID Thẻ" Binding="{Binding CardUid}" Width="*"/>
                        <DataGridTextColumn Header="Chủ thẻ" Binding="{Binding User.FullName}" Width="*"/>
                        <DataGridTextColumn Header="Ghi chú" Binding="{Binding CardLabel}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Hoạt động" Binding="{Binding IsActive}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="{StaticResource BgMedium}" Margin="0,10,10,10" CornerRadius="8">
            <StackPanel Margin="25">
                <TextBlock Text="Thông tin Thẻ RFID" Foreground="White" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,25"/>
                <TextBlock Text="Mã UID Thẻ" Foreground="#CCCCCC" FontSize="15"/>
                <TextBox Text="{Binding CardUid, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Chủ thẻ (Người dùng)" Foreground="#CCCCCC" FontSize="15"/>
                <ComboBox ItemsSource="{Binding UsersList}" SelectedItem="{Binding SelectedUser, Mode=TwoWay}" DisplayMemberPath="FullName"/>
                <TextBlock Text="Ghi chú / Nhãn dán" Foreground="#CCCCCC" FontSize="15"/>
                <TextBox Text="{Binding CardLabel, UpdateSourceTrigger=PropertyChanged}"/>
                <CheckBox Content="Đang hoạt động" IsChecked="{Binding IsActive}" Foreground="White" Margin="0,5,0,25" FontSize="15"/>

                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Row="0" Grid.Column="0" Content="Thêm mới" Command="{Binding AddCardCommand}" Style="{StaticResource ActionBtn}" Margin="0,0,5,10"/>
                    <Button Grid.Row="0" Grid.Column="1" Content="Cập nhật" Command="{Binding UpdateCardCommand}" Style="{StaticResource ActionBtn}" Margin="5,0,0,10"/>
                    <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Khóa thẻ" Command="{Binding DeleteCardCommand}" Style="{StaticResource DangerBtn}"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
