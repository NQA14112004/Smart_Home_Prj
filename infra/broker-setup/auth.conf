# Smart Home — require authentication, local-only listener.
# Bind to 127.0.0.1 so only this machine can connect; change to the LAN IP
# (or remove the address) later if ESP/Pi nodes must connect over the network.
listener 1883 127.0.0.1
allow_anonymous false
password_file C:\Program Files\Mosquitto\passwd
